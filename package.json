{
  "name": "grayzonecheats",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "python3 scripts/seo-optimize.py && node scripts/seo-verify.mjs",
    "pages:deploy": "npx wrangler pages deploy . --project-name=grayzonewarfare",
    "deploy": "npm run build && npm run pages:deploy",
    "deploy:local": "npm run build && npx wrangler login && npm run pages:deploy"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
